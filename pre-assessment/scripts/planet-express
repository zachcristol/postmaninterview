#!/usr/bin/env bash
# Runs the Planet Express CLI inside the compose stack (starts Postgres if needed).
# Example: ./scripts/planet-express create-shipment --destination=dst_mars
set -euo pipefail
cd "$(dirname "$0")/.."
exec docker compose run --rm --build -e SPRING_PROFILES_ACTIVE=cli backend "$@"
